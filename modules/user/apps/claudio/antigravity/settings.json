{
  "colorScheme": "tokyo night",
  "enableTelemetry": false,
  "permissions": {
    "allow": [
      "command(cat *)",
      "command(chmod *)",
      "command(command *)",
      "command(echo *)",
      "command(find *)",
      "command(fj *)",
      "command(git *)",
      "command(grep *)",
      "command(head *)",
      "command(jq *)",
      "command(just *)",
      "command(ls *)",
      "command(nix *)",
      "command(readlink *)",
      "command(rg *)",
      "command(rtk *)",
      "command(sort *)",
      "command(ssh -o ProxyCommand=)",
      "command(tail *)",
      "command(which *)"
    ],
    "ask": [],
    "deny": []
  },
  "verbosity": "low"
}
